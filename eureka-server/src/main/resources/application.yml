spring:
  application:
    name: eureka-server

  config:
    import:
      - optional:configserver:http://localhost:8888
      - optional:file:./env.properties

  cloud:
    config:
      fail-fast: true